# Supabase cloud sync (OPTIONAL).
# Leave BOTH unset to run in local-only mode (data stays in the browser).
# Set both to enable cloud sync; the app will then ask for a household code.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
